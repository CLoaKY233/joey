[project]
name = "joey"
version = "0.1.0"
description = "A masked-diffusion language model built from scratch in PyTorch"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "datasets>=4.8.5",
    "modal>=1.4.3",
    "numpy>=2.4.6",
    "pytest>=9.0.3",
    "tokenizers>=0.23.1",
    "torch>=2.12.0",
]
